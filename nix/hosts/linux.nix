{ config, pkgs, lib, ... }: {
  # # Add a server-only tool
  # home.packages = with pkgs; [ htop-vim ];
  #
  # # Example: override an env var only on this box
  # home.sessionVariables.MY_MACHINE_ROLE = "build-server";
}
